import { Router } from 'express';
// import { createItem, deleteItem, getItem, getItems, updateItem } from '../controllers/rdsController';

const rdsRouter = Router();

// rdsRouter.get('/', getItems);
// rdsRouter.get('/:id', getItem);
// rdsRouter.post('/', createItem);
// rdsRouter.put('/:id', updateItem);
// rdsRouter.delete('/:id', deleteItem);

export default rdsRouter;
